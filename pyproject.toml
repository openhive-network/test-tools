[build-system]
requires = [
    "setuptools == 63.1.0",
    "wheel == 0.37.1",
]
build-backend = "setuptools.build_meta"


[tool.black]
line-length = 120
skip-string-normalization = true


[tool.isort]
profile = "black"
line_length = 120
force_single_line = true
