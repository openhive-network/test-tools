[build-system]
requires = [
    "setuptools >= 42",
    "wheel",
]
build-backend = "setuptools.build_meta"


[tool.black]
line-length = 120
skip-string-normalization = true


[tool.isort]
line_length = 120
force_single_line = true
